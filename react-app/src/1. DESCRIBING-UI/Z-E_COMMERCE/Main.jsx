import React from 'react';
import SideBar from './SideBar';
export default function App(){
    return(
        <div style={{background : 'lineargradient(90deg,#ECEEF2,white)'}}>
            <SideBar/>
        </div>
    );
}
//This is similar to the setTimeOut Function bu t the only differnece is that in setTimOut function
//it gonna do the acction only once after the specified time but in setInterval it is different is executes everytime 
//after each time 
setInterval(() => {
    console.log("Hi there");
    
},2000);